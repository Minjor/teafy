<%= form_for(playlist_calification) do |f| %>
  <% if playlist_calification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(playlist_calification.errors.count, "error") %> prohibited this playlist_calification from being saved:</h2>

      <ul>
      <% playlist_calification.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :playlist_id %>
    <%= f.number_field :playlist_id %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :grade %>
    <%= f.number_field :grade %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
