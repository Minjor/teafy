<%= form_for(playlist_review) do |f| %>
  <% if playlist_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(playlist_review.errors.count, "error") %> prohibited this playlist_review from being saved:</h2>

      <ul>
      <% playlist_review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Post_a_review %>
    <%= f.text_area :review %>
  </div>
  <%= f.hidden_field :user_id, :value => current_user.id %>
  <%= f.hidden_field :playlist_id, :value => @playlist.id %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
