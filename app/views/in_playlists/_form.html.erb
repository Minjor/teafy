<%= form_for(in_playlist) do |f| %>
  <% if in_playlist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(in_playlist.errors.count, "error") %> prohibited this in_playlist from being saved:</h2>

      <ul>
      <% in_playlist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :song_id %>
    <%= f.number_field :song_id %>
  </div>

  <div class="field">
    <%= f.label :playlist_id %>
    <%= f.number_field :playlist_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
