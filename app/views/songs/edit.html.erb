<h1>Editing Song</h1>

<% if user_signed_in? && current_user.admin?%>
  <%= render 'form', song: @song %>

  <%= link_to 'Show', @song %> |
  <%= link_to 'Back', songs_path %>

<% else %>
  <p>You don't have permission to edit this song. Please go back to the homepage.</p>
<% end %>

<%= link_to 'Back', artists_path %>
