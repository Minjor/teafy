<%= form_for(song) do |f| %>
  <% if song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
      <% song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
     <%= f.label :artist %>
     <%= f.select :artist_id, options_for_select(Artist.all.collect { |a| [a.name, a.id] }) %>
  </div>

  <div class="field">
     <%= f.label :album %>
     <%= f.select :album_id, options_for_select(Album.all.collect { |a| [a.name, a.id] }) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
