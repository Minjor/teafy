<%= form_for(song_calification) do |f| %>
  <% if song_calification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(song_calification.errors.count, "error") %> prohibited this song_calification from being saved:</h2>

      <ul>
      <% song_calification.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.number_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :song_id %>
    <%= f.number_field :song_id %>
  </div>

  <div class="field">
    <%= f.label :grade %>
    <%= f.number_field :grade %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
